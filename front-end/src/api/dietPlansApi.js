// src/api/dietPlansApi.js
import httpClient from "./httpClient";

export async function getDietPlans() {
  // const res = await httpClient.get("/diet-plans");
  // return res.data;

  return Promise.resolve(null);
}
