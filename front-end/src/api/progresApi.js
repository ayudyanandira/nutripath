// src/api/progressApi.js
import httpClient from "./httpClient";

export async function getProgressList() {
  // const res = await httpClient.get("/progress");
  // return res.data;

  return Promise.resolve(null);
}

export async function createProgress(payload) {
  // const res = await httpClient.post("/progress", payload);
  // return res.data;

  return Promise.resolve(null);
}

